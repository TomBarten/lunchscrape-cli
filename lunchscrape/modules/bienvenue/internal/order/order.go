package order

import (
    "github.com/TomBarten/lunchscrape_cli/model/order"
)

func OrderItems(order *order.Order) {
    /*
       Use selenium to navigate to product pages
       Check if the order total cost is equal to the total cost represented in the shopping cart (on web-page)
    */
    panic("Not implemented")
}
